<h2>클래스(객체지향 관련 개념들)</h2>

- 클래스의 상속
  class Animal{
  constructor(public name: string){}
  move(distance: number): void{
  console.log(`${this.name} moved ${distance} meters.`);
  }
  }

class Bird extends Animal{
fly(): void{
console.log(`${this.name} is flying!`);
}
}

const sparrow = new Bird("Sparrow");
sparoow.move(10); ☞ Sparrow moved 10 meters.
sparrow.fly(); ☞ Sparrow is flying!

- 오버라이드(override)
  class Animal{
  constructor(public name: string){}
  speak(): void{
  conosle.log(`${this.name} makes a sound.`);
  }
  }

class Dog extends Animal{
speak(): void{
console.log(`${this.name} barks.`);
}
}
☞ 부모 클래스에서 정의한 같은 메소드를 자식 클래스에서 재정의
☞ 메소드 오버라이딩은 특정 자식 클래스가 특정 메소드의 작업을 부모와는 다르게 수행하고자 할 때 사용
